---
# verification jobs for 'xos' repo

- project:
    name: xos
    project: '{name}'

    jobs:
      - 'verify-xos-jobs':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'verify-xos-jobs'
    jobs:
      - 'verify-licensed'
      - 'verify-sonarqube':
          dependency-jobs: 'verify_xos_licensed'
      - 'xos-unit-test':
          dependency-jobs: 'verify_xos_sonarqube'
      - 'api-test':
          dependency-jobs: 'verify_xos_-unit-test'
