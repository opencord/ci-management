---
# maven job for 'mcast' repo

- project:
    name: mcast
    project: '{name}'

    jobs:
      - 'mcast-gerrit':
          branch-regexp: '^(.*)$'

- job-group:
    name: 'mcast-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
      - 'maven-deploy'
