---
# maven job for 'aaa' repo

- project:
    name: mcast
    project: '{name}'

    jobs:
      - 'mcast-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'mcast-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
