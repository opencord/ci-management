---
# maven job for 'aaa' repo

- project:
    name: igmp
    project: '{name}'

    jobs:
      - 'igmp-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'igmp-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
