---
# maven job for 'igmp' repo

- project:
    name: igmp
    project: '{name}'

    jobs:
      - 'igmp-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'igmp-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
