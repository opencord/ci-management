---
# maven job for 'igmp' repo

- project:
    name: igmp
    project: '{name}'

    jobs:
      - 'igmp-gerrit':
          branch-regexp: '^(.*)$'

- job-group:
    name: 'igmp-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
      - 'maven-deploy'
