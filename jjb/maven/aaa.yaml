---
# maven job for 'aaa' repo

- project:
    name: aaa
    project: '{name}'

    jobs:
      - 'aaa-gerrit-job':
          branch-regexp: '^(master|cord-5.0|cord-4.1|cord-4.0|aaa-[0-9]*\.[0-9]*|aaa-rework)$'

- job-group:
    name: 'aaa-gerrit-job'
    jobs:
      - 'maven-install'
