---
# maven job for 'dhcpl2relay' repo

- project:
    name: dhcpl2relay
    project: '{name}'

    jobs:
      - 'dhcpl2relay-gerrit':
          branch-regexp: '^(.*)$'

- job-group:
    name: 'dhcpl2relay-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
      - 'maven-deploy'
