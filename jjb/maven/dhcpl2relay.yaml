---
# maven job for 'aaa' repo

- project:
    name: dhcpl2relay
    project: '{name}'

    jobs:
      - 'dhcpl2relay-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'dhcpl2relay-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
