---
# maven job for 'aaa' repo

- project:
    name: igmpproxy
    project: '{name}'

    jobs:
      - 'igmpproxy-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'igmpproxy-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
