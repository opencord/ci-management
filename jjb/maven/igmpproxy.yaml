---
# maven job for 'igmpproxy' repo

- project:
    name: igmpproxy
    project: '{name}'

    jobs:
      - 'igmpproxy-gerrit':
          branch-regexp: '^(.*)$'

- job-group:
    name: 'igmpproxy-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
      - 'maven-deploy'
