---
# maven job for 'kafka-onos' repo

- project:
    name: kafka-onos
    project: '{name}'

    jobs:
      - 'kafka-onos-gerrit':
          branch-regexp: '^(.*)$'

- job-group:
    name: 'kafka-onos-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
      - 'maven-deploy'
