---
# maven job for 'olt' repo

- project:
    name: olt
    project: '{name}'

    jobs:
      - 'olt-gerrit':
          branch-regexp: '^(master|{name}-.*)$'

- job-group:
    name: 'olt-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
