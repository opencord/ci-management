---
# maven job for 'aaa' repo

- project:
    name: olt
    project: '{name}'

    jobs:
      - 'olt-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'olt-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
