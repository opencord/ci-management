---
# maven job for 'aaa' repo

- project:
    name: sadis
    project: '{name}'

    jobs:
      - 'sadis-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'sadis-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
