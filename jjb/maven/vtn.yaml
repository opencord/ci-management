---
# maven job for 'vtn' repo

- project:
    name: vtn
    project: '{name}'

    jobs:
      - 'vtn-gerrit':
          branch-regexp: '{supported-branches-regexp}'

- job-group:
    name: 'vtn-gerrit'
    jobs:
      - 'verify-licensed'
      - 'maven-install'
